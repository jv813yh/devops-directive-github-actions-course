name: Test Hello World

on: workflow_dispatch

jobs:
  say-hello-inline-bash:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Test Hello World from an inline bash script in a GitHub Action Workflow!"